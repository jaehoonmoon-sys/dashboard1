# 제작 원칙 — 트랙 저성과자 면담 관리 시스템

> 5시간 압박 환경에서 작품을 안정적으로 완성하기 위한 의사결정 기준.

## 1. MVP 우선순위

**한 가지 깊게 > 세 가지 얕게.** 5시간 안에 세 가지 욕심내면 셋 다 미완성으로 끝난다.

- ✅ 필수 1개: 데이터 종합 + 저성과자 유형 분리 (완성)
- ❌ 가능 1개: 면담록 시계열 DB (제외 — view 스키마로 토대만)
- ❌ 시간되면: 슬랙·에듀웍스 연동 (제외 — README에 향후 확장으로 명시)

## 2. 데이터 전략

- **실데이터 연동 X** — 노션/Redash gateway 경유 시 토큰·매핑·에러 처리에 시간 소진 위험
- **현실적 mock 시드** — 15명, 모든 위험 유형 케이스 포함 (정상·NPS낮음·운영불만·동료저평·메타인지갭·복합)
- 시연 시 "실제 연동은 같은 스키마 그대로, gateway endpoint만 교체" 라고 설명 가능한 구조

## 3. 분류 로직 — SQL view에 두기

- 비즈니스 로직을 **JS 코드가 아닌 SQL view (`mj_risk_candidates`)에 위치** — 매니저가 재무회계 마인드로 직접 SQL 수정 가능
- Next.js page는 view를 **단순 조회 + 표시**만 — 로직 분리로 변경 비용 최소화

## 4. 충돌 방지

- 모든 신규 테이블은 `mj_` prefix (moon_jaehoon) — 같은 team-1 조원과 Supabase public 충돌 회피
- 기존 `survey_results` 테이블은 **읽기만** — 다른 조원 작업 가능성 보호

## 5. 막혔을 때

- 5분 내 해결 안 되면 → 슬랙 본인 팀 채널 `@team-1-bot 막혔어요 [상황]`
- **16:30 이후** = 작품 완성보다 **4종 제출 우선** (README · 제작원칙 · 배포흔적 · 슬랙후기)

## 6. 보안

- `.env.local` 은 gitignored — 토큰 commit X
- Supabase publishable key (`sb_publishable_*`)는 RLS로 보호되는 read-only — 코드 fallback OK
- PII 저장 X — 시드 학생코드 모두 `STD_<hex>` 가명화 패턴

## 7. 시각화 결정

- 차트·그래프 X — 운영자가 보고 즉시 행동할 표 형태가 우선
- 운영진별 **3개 컬러 그룹** (운영기획=빨강, 학습관리=파랑, 튜터=보라) — 한 눈에 담당 인식
- 복합 위험은 빨간 뱃지로 강조 — "이거부터 봐주세요" 신호
